/* global describe,it */
'use strict';

import assert from 'assert';
import { DICTIONARY } from '../src/dictionary';
import * as UTILS from '../src/utils';

describe('utils', () => {
    describe('#escapeRegExp()', () => {
        it('should escape regular expression correctly', () => {
            assert.equal(-1, 1); // TODO: actually write tests
        });
    });

    describe('#replaceAll()', () => {
        it('should replace all regular expressions', () => {
            assert.equal(-1, 1); // TODO: actually write tests
        });
    });

    describe('#tokeniseRegExp()', () => {
        it('should tokenise regular expression', () => {
            assert.equal(-1, 1); // TODO: actually write tests
        });
    });
});
